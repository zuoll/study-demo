server:
  port: 8989
  servlet:
    context-path: /rate-limit

spring:
  redis:
    host: localhost
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
